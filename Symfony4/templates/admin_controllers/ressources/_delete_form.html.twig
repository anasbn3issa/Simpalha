{% block body %}

<form method="post" action="{{ path('ressources_delete', {'idr': ressource.idr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ressource.idr) }}">
    <button class="btn">Delete</button>
</form>
{% endblock%}
