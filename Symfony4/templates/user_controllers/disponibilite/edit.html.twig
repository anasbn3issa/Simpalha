{% extends 'baseUser.html.twig' %}
{% block title %}Edit Disponibilite{% endblock %}

{% block body %}
<div class="container">
<div class="row mb-4 mr-1 pb-4">
        <div class="col-12 mb-3">
            <a href="{{ path('user_controllers_disponibilite_index') }}"><button type="button" class="btn waves-effect waves-light btn-lg btn-primary">
                    &nbsp; Retour</button></a>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Create new Meet</h4>
                    <div class="card-block">
                        {{ include('user_controllers/disponibilite/_form.html.twig', {'button_label': 'Update'}) }}

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
