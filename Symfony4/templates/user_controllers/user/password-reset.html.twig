{% extends 'baseUser.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>reset password</h1>
                {{ form_start(form) }}
                
                {{ form_row(form.password.first) }}
                {{ form_row(form.password.second) }}

                <button type="submit" class="btn btn-default  btn-primary" formnovalidate>save</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
