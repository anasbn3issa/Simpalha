{% extends "baseAdmin.html.twig" %}

{% block body %}
    <h1>Candidatures</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Idc</th>
            <td>{{ candidature.idc }}</td>
        </tr>
        <tr>
            <th>Fichier</th>
            <td><a href="{{ asset('uploads/users/') }}{{ candidature.fichier }}" target="_blank">fichier</a></td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ candidature.status }}</td>
        </tr>
        <tr>
            <th>Datec</th>
            <td>{{ candidature.datec ? candidature.datec|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('candidatures_index') }}">back to list</a>

    <a href="{{ path('candidatures_edit', {'idc': candidature.idc}) }}">edit</a>

    {{ include('candidatures/_delete_form.html.twig') }}
{% endblock %}
