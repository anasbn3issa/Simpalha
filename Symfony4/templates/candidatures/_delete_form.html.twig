<form method="post" action="{{ path('candidatures_delete', {'idc': candidature.idc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidature.idc) }}">
    <button class="btn">Delete</button>
</form>
